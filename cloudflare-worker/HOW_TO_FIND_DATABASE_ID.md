# 🔍 Database ID 찾는 방법

## 📱 Cloudflare 대시보드에서 찾기 (가장 쉬움)

### 1단계: 로그인
```
https://dash.cloudflare.com
```

### 2단계: 메뉴 찾기
```
왼쪽 사이드바
└── Workers & Pages (클릭)
    └── D1 SQL Database (상단 탭 클릭)
```

### 3단계: 데이터베이스 선택
```
만든 데이터베이스 이름 클릭
예: "superplace-db"
```

### 4단계: Database ID 복사
```
화면에 표시된 Database ID 복사
형식: 긴 영문자+숫자 조합 (36자)
예: 1a2b3c4d-5e6f-7g8h-9i0j-k1l2m3n4o5p6
```

---

## 💻 명령어로 찾기 (개발자용)

터미널에서 실행:
```bash
cd cloudflare-worker
wrangler d1 list
```

결과:
```
📋 Database Name        Database ID
   superplace-db        1a2b3c4d-5e6f-7g8h-9i0j-k1l2m3n4o5p6
```

---

## ✅ 찾은 Database ID 확인

올바른 형식:
```
✅ 1a2b3c4d-5e6f-7g8h-9i0j-k1l2m3n4o5p6
✅ abc12345-def6-789g-hijk-lmnop1234567
```

잘못된 형식:
```
❌ superplace-db (이건 데이터베이스 이름)
❌ 123456 (너무 짧음)
❌ 빈 문자열
```

---

## 📝 찾은 ID를 어디에 입력?

파일: `cloudflare-worker/wrangler.toml`

10번째 줄 수정:
```toml
database_id = "여기에_복사한_Database_ID_붙여넣기"
```

저장!

---

## 🆘 Database ID를 찾을 수 없다면?

### 원인 1: 데이터베이스가 아직 없음
→ 새로 만들어야 함

```bash
wrangler d1 create superplace-db
```

### 원인 2: 다른 계정으로 만듦
→ 올바른 Cloudflare 계정으로 로그인 확인

### 원인 3: 삭제됨
→ 다시 만들어야 함
