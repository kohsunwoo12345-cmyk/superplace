name = "physonsuperplacestudy"
main = "python-worker-src.js"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# Worker 설정
workers_dev = true

# 메모리 및 CPU 제한 (Pyodide는 리소스 많이 사용)
[placement]
mode = "smart"

# 환경 변수 (필요시)
[vars]
ENVIRONMENT = "production"
