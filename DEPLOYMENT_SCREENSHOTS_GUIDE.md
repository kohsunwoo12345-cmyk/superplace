# 📸 Cloudflare 배포 - 화면별 입력 가이드

> 각 화면에서 무엇을 입력해야 하는지 알려드립니다!

---

## 🎯 Cloudflare Pages vs Workers 선택

### 화면에서 보이는 것:

```
┌─────────────────────────────────────────┐
│  Create an application                  │
├─────────────────────────────────────────┤
│                                         │
│  [Pages]        [Workers]               │
│                                         │
└─────────────────────────────────────────┘
```

**클릭할 것**: **Pages** (왼쪽)

---

## 📋 Pages 배포 화면

### 화면 1: Connect to Git

```
┌─────────────────────────────────────────┐
│  Create a new project                   │
├─────────────────────────────────────────┤
│                                         │
│  [Connect to Git]                       │
│                                         │
│  Connect your Git provider              │
│  to deploy from your repository         │
│                                         │
└─────────────────────────────────────────┘
```

**클릭할 것**: **Connect to Git** 버튼

---

### 화면 2: GitHub 선택

```
┌─────────────────────────────────────────┐
│  Connect Git provider                   │
├─────────────────────────────────────────┤
│                                         │
│  [GitHub]  [GitLab]  [Bitbucket]       │
│                                         │
└─────────────────────────────────────────┘
```

**클릭할 것**: **GitHub** 버튼

→ GitHub 로그인 화면이 열립니다
→ 로그인하고 "Authorize Cloudflare Pages" 클릭

---

### 화면 3: 저장소 선택

```
┌─────────────────────────────────────────┐
│  Select a repository                    │
├─────────────────────────────────────────┤
│  🔍 [Search repositories...        ]   │
│                                         │
│  📁 kohsunwoo12345-cmyk/superplace     │
│     [Select]                            │
│                                         │
└─────────────────────────────────────────┘
```

**입력할 것**: 
- 검색창에 `superplace` 입력
- **Select** 버튼 클릭

---

### 화면 4: 빌드 설정 ⭐ 중요!

```
┌──────────────────────────────────────────────────┐
│  Set up builds and deployments                   │
├──────────────────────────────────────────────────┤
│                                                  │
│  Project name                                    │
│  [                                           ]   │
│                                                  │
│  Production branch                               │
│  [main                                       ▼]  │
│                                                  │
│  Framework preset                                │
│  [Next.js                                    ▼]  │
│                                                  │
│  Build command                                   │
│  [                                           ]   │
│                                                  │
│  Build output directory                          │
│  [                                           ]   │
│                                                  │
│  Root directory (optional)                       │
│  [/                                          ]   │
│                                                  │
│         [Cancel]  [Save and Deploy]              │
└──────────────────────────────────────────────────┘
```

**입력할 내용:**

**Project name** (프로젝트 이름):
```
superplace_study
```
복사해서 붙여넣기 ▶️

**Production branch** (드롭다운):
```
main
```
드롭다운에서 선택 ▶️

**Framework preset** (드롭다운):
```
Next.js
```
드롭다운에서 선택 ▶️

**Build command** (빌드 명령):
```
npm run build
```
복사해서 붙여넣기 ▶️

**Build output directory** (출력 디렉토리):
```
.next
```
복사해서 붙여넣기 ▶️ (점 포함!)

**Root directory (optional)** (루트 디렉토리):
```
/
```
슬래시 하나만 입력 (또는 비워둠)

---

**클릭할 것**: **Save and Deploy** 버튼 (파란색)

---

### 화면 5: 배포 진행 중

```
┌──────────────────────────────────────────┐
│  Building your project...                │
├──────────────────────────────────────────┤
│  ●●●●●●○○○○ 60%                        │
│                                          │
│  Initializing build environment          │
│  ✓ Cloning repository                    │
│  ✓ Installing dependencies               │
│  ● Building application...               │
│                                          │
└──────────────────────────────────────────┘
```

**할 것**: 기다리기 (2-5분 소요)

---

### 화면 6: 배포 완료!

```
┌──────────────────────────────────────────┐
│  ✓ Success! Your site is live!           │
├──────────────────────────────────────────┤
│  https://superplace-study.pages.dev      │
│  [Visit site]  [Continue to project]     │
└──────────────────────────────────────────┘
```

**클릭할 것**: **Continue to project** 버튼

---

## 🔐 환경 변수 추가 (필수!)

### 화면 7: 프로젝트 설정

```
┌──────────────────────────────────────────┐
│  superplace-study                        │
├──────────────────────────────────────────┤
│  [Deployments] [Settings] [Analytics]    │
│                                          │
└──────────────────────────────────────────┘
```

**클릭할 것**: **Settings** 탭

---

### 화면 8: Settings 메뉴

```
┌──────────────────────────────────────────┐
│  Settings                                │
├──────────────────────────────────────────┤
│  General                                 │
│  Functions                               │
│  Environment variables        ◀️ 여기!   │
│  Builds & deployments                    │
│  Redirects & Headers                     │
└──────────────────────────────────────────┘
```

**클릭할 것**: **Environment variables**

---

### 화면 9: 환경 변수 추가

```
┌──────────────────────────────────────────┐
│  Environment variables                   │
├──────────────────────────────────────────┤
│  [Add variable]                          │
│                                          │
│  No environment variables yet            │
│                                          │
└──────────────────────────────────────────┘
```

**클릭할 것**: **Add variable** 버튼

---

### 화면 10: 첫 번째 변수 추가

```
┌──────────────────────────────────────────┐
│  Add environment variable                │
├──────────────────────────────────────────┤
│  Variable name                           │
│  [                                    ]  │
│                                          │
│  Value                                   │
│  [                                    ]  │
│                                          │
│  Environment                             │
│  ☑️ Production                           │
│  ☑️ Preview                              │
│                                          │
│         [Cancel]  [Save]                 │
└──────────────────────────────────────────┘
```

**입력할 내용 (첫 번째 변수):**

Variable name:
```
DATABASE_URL
```

Value:
```
file:./prisma/dev.db
```

Environment:
- ✅ Production 체크
- ✅ Preview 체크

**클릭**: **Save** 버튼

---

**두 번째 변수 추가:** 다시 **Add variable** 클릭

Variable name:
```
NEXTAUTH_URL
```

Value:
```
https://superplace-study.pages.dev
```

Environment:
- ✅ Production 체크
- ✅ Preview 체크

**클릭**: **Save** 버튼

---

**세 번째 변수 추가:** 다시 **Add variable** 클릭

Variable name:
```
NEXTAUTH_SECRET
```

Value:
```
[여기에 생성한 비밀 키 붙여넣기]
```

**비밀 키 생성하는 곳:**
https://generate-secret.vercel.app/32

Environment:
- ✅ Production 체크
- ✅ Preview 체크

**클릭**: **Save** 버튼

---

## 🔄 재배포하기 (필수!)

### 화면 11: Deployments 탭

```
┌──────────────────────────────────────────┐
│  [Deployments] [Settings] [Analytics]    │
├──────────────────────────────────────────┤
│  Latest deployments                      │
│                                          │
│  ✓ Production                            │
│    main@abc123  2 minutes ago            │
│    [···]  ◀️ 여기 클릭!                 │
│                                          │
└──────────────────────────────────────────┘
```

**클릭할 것**: 
1. **Deployments** 탭
2. 최신 배포 항목의 **···** (점 세개)
3. **Retry deployment** 또는 **Redeploy** 선택

---

## ✅ 완료 확인

### 화면 12: 배포 완료

```
┌──────────────────────────────────────────┐
│  ✓ Deployment successful                 │
├──────────────────────────────────────────┤
│  https://superplace-study.pages.dev      │
│  [Visit site]                            │
└──────────────────────────────────────────┘
```

**클릭할 것**: **Visit site** 버튼

→ 웹사이트가 열리면 성공! 🎉

---

## 🎯 요약 체크리스트

배포 과정에서 입력/선택한 것들:

- [x] GitHub 저장소: `superplace` 선택
- [x] 프로젝트 이름: `superplace_study` 입력
- [x] 브랜치: `main` 선택
- [x] 프레임워크: `Next.js` 선택
- [x] 빌드 명령: `npm run build` 입력
- [x] 출력 디렉토리: `.next` 입력
- [x] 환경 변수 3개 추가
  - [x] DATABASE_URL
  - [x] NEXTAUTH_URL
  - [x] NEXTAUTH_SECRET
- [x] 재배포(Redeploy) 실행

모두 완료했다면 배포 성공! 🎊

---

## 📱 이 문서 활용 방법

1. **프린트하기**: 종이로 출력해서 옆에 두기
2. **스크린샷**: 스마트폰에 저장하기
3. **다른 모니터**: 다른 화면에 띄워놓기

**스마트폰으로 이 문서를 보면서 컴퓨터로 작업하면 편해요!**

---

**질문 있으면 언제든 물어보세요!** 😊
