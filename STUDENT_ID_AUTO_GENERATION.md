# ✅ 학생 고유 번호 자동 부여 기능 구현 완료

## 🎯 구현 완료

학생 추가 또는 회원가입 시 **자동으로 고유한 학번(Student ID)**을 부여하는 시스템을 구축했습니다!

---

## 📋 주요 기능

### 1. **학번 자동 생성**
- **형식**: `STU-001`, `STU-002`, `STU-003`, ...
- **특징**:
  - 순차적 번호 부여
  - 학원별로 독립적 관리
  - 중복 방지 (자동 재시도)
  - 3자리 숫자 (앞에 0 채우기)

### 2. **학생 코드와 함께 부여**
각 학생은 가입/추가 시 다음 두 가지를 자동으로 받습니다:
- **학번 (studentId)**: `STU-001` (학원 내 고유 식별자)
- **학생 코드 (studentCode)**: `12345` (5자리, 로그인용)

---

## 🔧 구현 내용

### 1. **학번 생성 함수** (`src/lib/student-code.ts`)

```typescript
/**
 * 학원별 순차적인 학번 생성
 * @param academyId 학원 ID (없으면 전체 학생 중 최대값 사용)
 * @returns 학번 (예: STU-001, STU-002, ...)
 */
export async function generateUniqueStudentId(academyId?: string | null): Promise<string>
```

#### 작동 방식:
1. **학원별 마지막 학번 조회**:
   - 해당 학원의 학생 중 가장 큰 학번을 찾음
   - 학원이 없으면 전체 학생 중 최대값 사용

2. **다음 번호 계산**:
   - 기존 학번에서 숫자 부분 추출 (`STU-001` → `1`)
   - 1을 더해서 다음 번호 생성 (`1` → `2`)

3. **새 학번 생성**:
   - 3자리 숫자로 포맷팅 (`2` → `002`)
   - 접두사 추가 (`002` → `STU-002`)

4. **중복 체크**:
   - DB에서 중복 확인
   - 중복이 있으면 재귀 호출로 다음 번호 생성

5. **오류 처리**:
   - 오류 발생 시 타임스탬프 기반 학번 생성
   - 예: `STU-123456` (현재 시간의 마지막 6자리)

---

### 2. **학생 추가 API** (`src/app/api/students/create/route.ts`)

#### 변경 사항:
```typescript
// Before
const studentCode = await generateUniqueStudentCode();

const student = await prisma.user.create({
  data: {
    // ...
    studentCode, // 학생 코드만 생성
  },
});

// After
const studentCode = await generateUniqueStudentCode();
const studentId = await generateUniqueStudentId(director.academyId); // ✅ 추가

const student = await prisma.user.create({
  data: {
    // ...
    studentId, // ✅ 학번 할당
    studentCode, // 학생 코드 할당
  },
});
```

#### 응답 데이터:
```json
{
  "message": "학생 계정이 생성되었습니다.",
  "student": {
    "id": "cm...",
    "email": "student@example.com",
    "name": "홍길동",
    "studentId": "STU-001",  // ✅ 학번
    "studentCode": "12345"    // 학생 코드
  },
  "studentId": "STU-001",
  "studentCode": "12345"
}
```

---

### 3. **회원가입 API** (`src/app/api/register/route.ts`)

#### 학생 회원가입 시 자동 생성:
```typescript
// 학생인 경우 학번과 학생 코드 자동 생성
let studentId = undefined;
let studentCode = undefined;

if (validatedData.role === 'STUDENT') {
  console.log('🔢 Generating student ID and code...');
  studentId = await generateUniqueStudentId(academy.id);
  studentCode = await generateUniqueStudentCode();
  console.log(`✅ Generated student ID: ${studentId}, Code: ${studentCode}`);
}

const user = await prisma.user.create({
  data: {
    // ...
    studentId, // ✅ 학생인 경우에만 값이 있음
    studentCode, // ✅ 학생인 경우에만 값이 있음
  },
});
```

#### 응답 데이터:
```json
{
  "message": "OO학원에 가입 신청이 완료되었습니다. 학원장 승인 후 로그인하실 수 있습니다.",
  "user": {
    "id": "cm...",
    "email": "student@example.com",
    "name": "홍길동",
    "studentId": "STU-001",  // ✅ 학번
    "studentCode": "12345"    // 학생 코드
  },
  "studentId": "STU-001",
  "studentCode": "12345",
  "academyName": "OO학원",
  "pendingApproval": true
}
```

---

### 4. **학생 관리 페이지 UI** (`src/app/dashboard/students/page.tsx`)

#### 표시 순서:
```
📧 이메일
🆔 학번: STU-001       ← ✅ 새로 추가
🔢 코드: 12345         ← 기존
📞 전화번호
🎓 학년
```

#### UI 코드:
```typescript
<div className="space-y-1 text-sm text-gray-600">
  <div className="flex items-center gap-2">
    <Mail className="w-4 h-4" />
    <span>{student.email}</span>
  </div>
  
  {/* ✅ 학번 표시 (새로 추가) */}
  {student.studentId && (
    <div className="flex items-center gap-2">
      <span className="font-mono text-indigo-600 font-semibold">
        🆔 학번: {student.studentId}
      </span>
    </div>
  )}
  
  {/* 학생 코드 표시 */}
  {student.studentCode && (
    <div className="flex items-center gap-2">
      <span className="font-mono text-blue-600 font-bold">
        🔢 코드: {student.studentCode}
      </span>
    </div>
  )}
  
  {/* ... 나머지 정보 */}
</div>
```

---

## 🌐 배포 정보

- **배포 URL**: https://superplace-study.vercel.app
- **학생 관리 페이지**: https://superplace-study.vercel.app/dashboard/students
- **GitHub**: https://github.com/kohsunwoo12345-cmyk/superplace
- **커밋**: `0d39986` - feat: 학생에게 고유 학번 자동 부여 기능 구현
- **빌드**: ✅ 성공
- **배포**: ✅ 완료 (약 2-3분 소요)

---

## ✅ 즉시 테스트

### 1️⃣ 기존 학생 확인
1. **로그인**: https://superplace-study.vercel.app/auth/signin
   - 계정: `director@ggume.com` (또는 관리자/학원장 계정)

2. **학생 관리 페이지로 이동**:
   - https://superplace-study.vercel.app/dashboard/students

3. **학생 정보 확인**:
   - 각 학생 카드에서 다음 정보 확인:
     - `🆔 학번: STU-XXX` (기존 학생은 학번이 없을 수 있음)
     - `🔢 코드: XXXXX` (5자리 코드)

### 2️⃣ 새 학생 추가 테스트
1. **"학생 추가" 버튼 클릭**:
   - 우측 상단 "학생 추가" 버튼

2. **학생 정보 입력**:
   - 이메일: `newstudent@example.com`
   - 비밀번호: `password123`
   - 이름: `김철수`
   - 학년: `중1`

3. **저장 후 확인**:
   - 학생 목록에서 새 학생 카드 확인
   - `🆔 학번: STU-001` (또는 다음 순서 번호)
   - `🔢 코드: XXXXX` (5자리 코드)

### 3️⃣ 회원가입 테스트
1. **회원가입 페이지**: https://superplace-study.vercel.app/register

2. **학생으로 가입**:
   - 역할: 학생
   - 학원 코드: (학원장에게 받은 8자리 코드)
   - 이름, 이메일, 비밀번호 입력

3. **가입 완료 후**:
   - 응답에서 `studentId`와 `studentCode` 확인
   - 예: `"studentId": "STU-002"`, `"studentCode": "67890"`

4. **학원장 승인 후**:
   - 학생 관리 페이지에서 승인
   - 학생 카드에서 학번과 코드 확인

---

## 📊 학번과 학생 코드 비교

| 항목 | 학번 (studentId) | 학생 코드 (studentCode) |
|------|------------------|-------------------------|
| **형식** | `STU-001`, `STU-002`, ... | `12345` (5자리 숫자) |
| **용도** | 학원 내 고유 식별자 | 로그인 및 숙제 제출용 |
| **관리** | 학원별 순차 부여 | 전체 시스템에서 고유 |
| **표시** | 학생 관리 페이지 | 학생 관리 + 로그인 페이지 |
| **변경** | 불가 (고정) | 불가 (재발급 가능) |
| **사용 예** | 출석부, 성적표 | 로그인, 숙제 검사 |

---

## 🔄 전체 플로우

### 학생 추가 플로우 (관리자/학원장)
```
1. 학생 관리 페이지 접속
2. "학생 추가" 버튼 클릭
3. 학생 정보 입력
4. 저장 버튼 클릭
   ↓
5. 시스템이 자동으로 생성:
   - 학번: STU-001 (순차 번호)
   - 학생 코드: 12345 (5자리)
   ↓
6. DB에 저장
7. 학생 목록에 표시:
   - 🆔 학번: STU-001
   - 🔢 코드: 12345
```

### 학생 회원가입 플로우
```
1. 회원가입 페이지 접속
2. 역할: 학생 선택
3. 학원 코드 입력 (예: ABC12345)
4. 학생 정보 입력
5. 가입 버튼 클릭
   ↓
6. 시스템이 자동으로 생성:
   - 학번: STU-002 (다음 순서)
   - 학생 코드: 67890 (5자리)
   ↓
7. DB에 저장 (승인 대기 상태)
8. 응답에 학번과 코드 반환
   ↓
9. 학원장 승인 후
10. 학생 로그인 가능:
    - 이메일/비밀번호로 로그인 OR
    - 학생 코드(67890)로 로그인
```

---

## 🎨 학생 관리 페이지 UI 예시

```
┌─────────────────────────────────────────────────────┐
│ 홍길동                   [승인됨]  [학교: OO중학교]  │
├─────────────────────────────────────────────────────┤
│ 📧 student@example.com                              │
│ 🆔 학번: STU-001          ← ✅ 새로 추가!          │
│ 🔢 코드: 12345                                      │
│ 📞 010-1234-5678                                    │
│ 🎓 중1                                              │
├─────────────────────────────────────────────────────┤
│ 학부모: 010-9876-5432                               │
│ 포인트: 100P                                        │
│ 가입일: 2026-01-25                                  │
├─────────────────────────────────────────────────────┤
│ AI 봇 권한:                                         │
│ ☑ AI 채팅  ☑ AI 숙제  ☑ AI 학습                   │
├─────────────────────────────────────────────────────┤
│              [상세 보기]                            │
└─────────────────────────────────────────────────────┘
```

---

## 📝 활동 로그 기록

학생 추가 시 활동 로그에 다음과 같이 기록됩니다:

```json
{
  "action": "STUDENT_ADD",
  "resource": "STUDENTS",
  "description": "OO님이 학생 '홍길동'을(를) 추가했습니다. (학번: STU-001, 학생코드: 12345)",
  "metadata": {
    "studentEmail": "student@example.com",
    "studentName": "홍길동",
    "studentId": "STU-001",  // ✅ 학번 기록
    "studentCode": "12345",   // 학생 코드 기록
    "grade": "중1",
    "school": "OO중학교"
  }
}
```

---

## 🔐 보안 및 제약사항

### 학번 (studentId)
- ✅ 학원별로 독립적 관리
- ✅ 순차적 번호 부여로 추적 용이
- ✅ 중복 방지 (DB unique 제약)
- ✅ 변경 불가 (영구 식별자)

### 학생 코드 (studentCode)
- ✅ 전체 시스템에서 고유
- ✅ 5자리 숫자 (10000~99999)
- ✅ 중복 방지 (최대 100회 재시도)
- ✅ 로그인 전용 (보안 강화)

---

## 🎯 사용 시나리오

### 시나리오 1: 새 학생 등록
```
[학원장]
1. 학생 관리 > "학생 추가" 클릭
2. 학생 정보 입력 (이름, 이메일, 학년 등)
3. 저장 → 시스템이 자동으로 학번(STU-001)과 코드(12345) 생성

[학원장 → 학생/학부모]
4. 학생에게 다음 정보 전달:
   - 로그인 URL: https://superplace-study.vercel.app/homework-check
   - 학생 코드: 12345

[학생]
5. 로그인 URL 접속
6. 학생 코드(12345) 입력
7. 로그인 성공 → 숙제 제출
```

### 시나리오 2: 학생 자가 회원가입
```
[학생]
1. 회원가입 페이지 접속
2. 역할: 학생 선택
3. 학원 코드 입력 (학원장에게 받음)
4. 정보 입력 후 가입
   → 시스템이 자동으로 학번(STU-002)과 코드(67890) 생성

[학원장]
5. 학생 관리 > 승인 대기 목록 확인
6. 학생 정보 확인:
   - 🆔 학번: STU-002
   - 🔢 코드: 67890
7. "승인" 버튼 클릭

[학생]
8. 승인 완료 알림 수신
9. 학생 코드(67890)로 로그인
```

### 시나리오 3: 출석부 작성
```
[선생님]
1. 학생 관리 페이지에서 학생 목록 확인
2. 각 학생의 학번 확인:
   - 홍길동: STU-001
   - 김철수: STU-002
   - 이영희: STU-003
3. 출석부에 학번 기록
4. 성적 입력 시 학번으로 식별
```

---

## 🚀 향후 개선 가능 사항

1. **학번 형식 커스터마이징**:
   - 학원별로 학번 접두사 설정 (예: `ABC-001`, `XYZ-001`)
   - 학년별 구분 (예: `STU1-001`, `STU2-001`)

2. **학번 재발급**:
   - 학생 전학 시 학번 재사용 또는 폐기
   - 학번 변경 이력 관리

3. **학번 검색 기능**:
   - 학번으로 학생 검색
   - 학번 범위 필터링 (예: STU-001 ~ STU-050)

4. **학번 통계**:
   - 학원별 학생 수 추이
   - 학번 발급 내역

5. **학번 인쇄**:
   - 학생증 출력 (QR 코드 포함)
   - 출석부 PDF 생성

---

## 📋 완료 체크리스트

- [x] `generateUniqueStudentId()` 함수 구현
- [x] 학생 추가 API에서 학번 자동 생성
- [x] 회원가입 API에서 학번 자동 생성 (학생만)
- [x] 학생 관리 페이지 UI에 학번 표시
- [x] 활동 로그에 학번 기록
- [x] 중복 방지 로직 구현
- [x] 오류 처리 (타임스탬프 기반 학번)
- [x] 빌드 성공
- [x] Git 커밋 및 푸시
- [x] Vercel 배포

---

## 🎉 최종 완료!

**이제 모든 학생은 가입/추가 시 자동으로 다음을 받습니다:**

- ✅ **학번 (studentId)**: `STU-001`, `STU-002`, ... (학원 내 고유 식별자)
- ✅ **학생 코드 (studentCode)**: `12345`, `67890`, ... (5자리, 로그인용)

### 즉시 확인:
1. https://superplace-study.vercel.app/dashboard/students
2. "학생 추가" 버튼으로 신규 학생 등록
3. 학생 카드에서 학번과 코드 확인

**배포**: https://superplace-study.vercel.app
**GitHub**: https://github.com/kohsunwoo12345-cmyk/superplace

추가 기능이 필요하시면 언제든 말씀해주세요! 🚀

---

**작성일**: 2026-01-25
**작성자**: Claude AI Assistant
**커밋**: 0d39986
