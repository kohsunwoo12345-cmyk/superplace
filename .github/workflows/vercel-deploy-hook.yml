name: Trigger Vercel Deploy Hook

# main 또는 genspark_ai_developer 브랜치에 푸시될 때 실행
on:
  push:
    branches:
      - main
      - genspark_ai_developer

jobs:
  trigger-deployment:
    runs-on: ubuntu-latest
    
    steps:
      - name: Vercel Deploy Hook 트리거
        run: |
          echo "🚀 Vercel 배포 트리거 시작..."
          
          # Deploy Hook URL이 설정되어 있는 경우에만 실행
          if [ -n "${{ secrets.VERCEL_DEPLOY_HOOK_URL }}" ]; then
            curl -X POST "${{ secrets.VERCEL_DEPLOY_HOOK_URL }}"
            echo "✅ Vercel 배포가 트리거되었습니다!"
          else
            echo "⚠️  VERCEL_DEPLOY_HOOK_URL이 설정되지 않았습니다."
            echo "Vercel Dashboard → Settings → Git → Deploy Hooks에서 생성하세요."
          fi
